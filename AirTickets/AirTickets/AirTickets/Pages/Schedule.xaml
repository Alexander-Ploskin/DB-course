<UserControl x:Class="AirTickets.Pages.Schedule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:AirTickets.ViewModel"
             xmlns:local="clr-namespace:AirTickets.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:ScheduleViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel>
            <DataGrid ItemsSource="{Binding VisibleSchedule}" IsReadOnly="True" MaxWidth="750" HorizontalAlignment="Left" x:Name="schedule"/>
            <DataGrid ItemsSource="{Binding SelectedFlightData}" IsReadOnly="True" MaxWidth="400" HorizontalAlignment="Right"/>
            <TextBlock Text="{Binding Message}"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="&lt;" Command="{Binding MoveLeftCommand}"/>
                <Button Content=">" Command="{Binding MoveRightCommand}"/>
            </StackPanel>
            <Button Content="Get data" Command="{Binding GetDataCommand}"/>
            <Button Content="Connect" Command="{Binding ConnectCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
